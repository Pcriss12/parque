(function() {
    'use strict'
    angular
        .module('park')
        .controller('reserveController', reserveController);
        reserveController.$inject = ['$http']; 

    function reserveController($http) {
       
    }
})();
